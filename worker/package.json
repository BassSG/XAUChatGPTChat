{
  "name": "xauchatgptchat-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create xau-chatgptchat-db",
    "db:migrate:local": "wrangler d1 migrations apply DB --local",
    "db:migrate:remote": "wrangler d1 migrations apply DB --remote",
    "vapid": "node scripts/configure-secrets.mjs"
  },
  "dependencies": {
    "@mmmike/web-push": "1.3.0"
  },
  "devDependencies": {
    "wrangler": "4.140.0"
  }
}
